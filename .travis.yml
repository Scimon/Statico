os:
  - linux
  - osx
language: perl6
perl6:
  - latest
  - '2017.06'
install:
  - rakudobrew build zef
  - zef --debug --depsonly install .
script:
  - PERL6LIB=lib prove -e perl6 -vr t/
sudo: false
